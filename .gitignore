# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.docker

# Test/coverage artifacts (generated in CI)
coverage/
*.junit.xml

# Generated data bootstrap/export zips (large, regenerate via Data → Export)
evalmvp-*.zip

# Large local datasets — only generations_output.csv and the criteria Excel
# file are read by the app (db/seed.py); the rest never belongs in git
data/cocacola_extracted/
data/generations_coke/
data
data/*.zip
db/evals.db
server/results/
__pycache__/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
data/*.json
evalmvp-data-bootstrap/*
data/

# Local debug/scratch output (DB dumps, etc.)
debug/

# Local secrets & tooling — never commit
/env
.codegraph/
